[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "rosclaw-ur-ros2-mcp"
version = "0.1.0"
description = "ROSClaw MCP Server for Universal Robots UR5/UR5e via ROS2"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
keywords = ["mcp", "robotics", "ur5", "ros2", "universal-robots"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "mcp[fastmcp]>=1.0.0",
]

[project.optional-dependencies]
ros2 = [
    # rclpy is installed via ROS2 distribution
    # source /opt/ros/humble/setup.bash
]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21",
    "ruff>=0.1.0",
]

[project.scripts]
rosclaw-ur-mcp = "ur_mcp_server:main"

[project.urls]
Homepage = "https://github.com/ros-claw/rosclaw-ur-ros2-mcp"
Repository = "https://github.com/ros-claw/rosclaw-ur-ros2-mcp"
Issues = "https://github.com/ros-claw/rosclaw-ur-ros2-mcp/issues"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
